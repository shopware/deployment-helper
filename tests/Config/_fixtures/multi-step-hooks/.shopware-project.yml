deployment:
  hooks:
    pre: |
      echo "Before deployment"
    post:
      - title: Warm up the cache
        script: |
          bin/console cache:warmup
      - title: Notify the team
        script: ./notify.sh
