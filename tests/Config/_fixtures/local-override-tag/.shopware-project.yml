deployment:
  hooks:
    pre: |
      echo "Before deployment"
    post: |
      echo "After deployment"

  extension-management:
    enabled: true
    exclude:
      - Name

  store:
    license-domain: prod.example.com
