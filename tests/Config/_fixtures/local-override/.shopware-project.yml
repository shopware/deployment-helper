deployment:
  hooks:
    pre: |
      echo "Before deployment general"
    post: |
      echo "After deployment general"

  extension-management:
    enabled: true
    exclude:
      - Name

  one-time-tasks:
    - id: foo
      script: test
